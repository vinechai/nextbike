# local development (docker postgres)
DATABASE_URL=postgresql://nextbike:nextbike@localhost:5432/nextbike

# supabase (for github actions — set as repository secret, not here)
# DATABASE_URL=postgresql://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres?sslmode=require

POSTGRES_USER=nextbike
POSTGRES_PASSWORD=nextbike
POSTGRES_DB=nextbike
POSTGRES_PORT=5432

PGADMIN_EMAIL=admin@nextbike.local
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050
